#pragma once

namespace nZemax
{
    enum Params {
        windowHeight = 600,
        windowWidth  = 600,
        sphereRadius = 200,
    };

    enum Colors {
        royalBlue = 0xe16941,
    };
};
