#pragma once

namespace nZemax
{
    enum Params {
        windowHeight = 600,
        windowWidth  = 600,
        sphereRadius = 200,
    };

    enum Colors: unsigned int {
        royalBlue = 0xE16941,
        cherry    = 0x2D04D2,
    };
};
